<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cookandroid.carrot_market">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Carrot_Market">
        <activity android:name=".MainActivity"/>
        <activity android:name=".VertificationActivity"/>
        <activity android:name=".Vertification2Activity"/>
        <activity android:name=".AddressActivity"/>
        <activity android:name=".BeginActivity"/>
        <activity android:name=".SystemSettingActivity"/>
        <activity android:name=".IntroActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>